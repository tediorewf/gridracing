
export default class Car {
    constructor(x, y, angle) {
        this.x = x;
        this.y = y;
        this.angle = angle;
    }

    move(x, y, angle) {
        this.x = x;
        this.y = y;
        this.angle = angle;
    }
}
